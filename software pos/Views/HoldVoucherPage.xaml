<UserControl x:Class="RashanKiDukan.Views.HoldVoucherPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="52"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <Border Grid.Row="0" Background="White" BorderBrush="#E5E7EB" BorderThickness="0,0,0,1">
            <Grid Margin="20,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Content="&#xE72B;" FontFamily="Segoe MDL2 Assets" FontSize="18"
                        Background="Transparent" Foreground="#6B7280" BorderThickness="0"
                        Cursor="Hand" VerticalAlignment="Center" Click="BtnBack_Click" Padding="6,4"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="12,0,0,0">
                    <TextBlock Text="Hold Vouchers" FontSize="17" FontWeight="SemiBold"
                               Foreground="#111827" FontFamily="Inter, Segoe UI" VerticalAlignment="Center"/>
                    <Border Background="#F3F4F6" CornerRadius="10" Padding="10,3" Margin="10,0,0,0" VerticalAlignment="Center">
                        <TextBlock x:Name="lblCount" Text="0 bills" FontSize="12" Foreground="#6B7280" FontFamily="Inter, Segoe UI"/>
                    </Border>
                </StackPanel>
                <Button Grid.Column="2" Content="Clear All" FontSize="12" FontWeight="SemiBold" Background="#EF4444"
                        Foreground="White" BorderThickness="0" Cursor="Hand" Padding="14,6"
                        VerticalAlignment="Center" Click="BtnClearAll_Click"/>
            </Grid>
        </Border>

        <!-- HOLD LIST -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="holdList" Margin="20,16"/>
        </ScrollViewer>
    </Grid>
</UserControl>

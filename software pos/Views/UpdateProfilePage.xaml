<UserControl x:Class="RashanKiDukan.Views.UpdateProfilePage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             xmlns:helpers="clr-namespace:RashanKiDukan.Helpers" d:DesignHeight="900" d:DesignWidth="1200">
    <UserControl.Resources>
        <!-- Text input -->
        <Style x:Key="InputStyle" TargetType="TextBox">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="#111827"/>
            <Setter Property="FontFamily" Value="Inter, Segoe UI"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ScrollViewer x:Name="PART_ContentHost" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="BorderBrush" Value="#A5B4FC"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="bd" Property="BorderBrush" Value="#6366F1"/>
                                <Setter TargetName="bd" Property="BorderThickness" Value="1.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Password input -->
        <Style x:Key="PwdStyle" TargetType="PasswordBox">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#111827"/>
            <Setter Property="FontFamily" Value="Inter, Segoe UI"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border x:Name="bd" Background="White" BorderBrush="#D1D5DB" BorderThickness="1" CornerRadius="8" Padding="12,10">
                            <ScrollViewer x:Name="PART_ContentHost" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="BorderBrush" Value="#A5B4FC"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="bd" Property="BorderBrush" Value="#6366F1"/>
                                <Setter TargetName="bd" Property="BorderThickness" Value="1.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Combo -->
        <Style x:Key="ComboStyle" TargetType="ComboBox">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="#111827"/>
            <Setter Property="FontFamily" Value="Inter, Segoe UI"/>
            <Setter Property="Background" Value="White"/>
        </Style>

        <!-- Primary button -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#6366F1"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontFamily" Value="Inter, Segoe UI"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="18,10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#4F46E5"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#4338CA"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Secondary button -->
        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#374151"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontFamily" Value="Inter, Segoe UI"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}" BorderBrush="#D1D5DB" BorderThickness="1" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#F3F4F6"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#E5E7EB"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Card -->
        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="#F8FAFC">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <Border Grid.Row="0" Background="#FFFFFF" BorderBrush="#E5E7EB" BorderThickness="0,0,0,1" Padding="28,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="Update Profile" FontSize="18" FontWeight="Bold" Foreground="#111827" FontFamily="Inter, Segoe UI"/>
                    <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                        <TextBlock Text="Home" FontSize="12" Foreground="#6B7280" FontFamily="Inter, Segoe UI" VerticalAlignment="Center"/>
                        <TextBlock Text=" > " FontSize="12" Foreground="#9CA3AF" VerticalAlignment="Center"/>
                        <TextBlock Text="Profile" FontSize="12" Foreground="#6B7280" FontFamily="Inter, Segoe UI" VerticalAlignment="Center"/>
                        <TextBlock Text=" > " FontSize="12" Foreground="#9CA3AF" VerticalAlignment="Center"/>
                        <TextBlock Text="Update Profile" FontSize="12" Foreground="#6B7280" FontFamily="Inter, Segoe UI" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    </StackPanel>
                </StackPanel>
                <StackPanel Grid.Column="1" VerticalAlignment="Center" Orientation="Horizontal">
                    <TextBlock x:Name="txtLoggedInAs" FontSize="12" Foreground="#6B7280" FontFamily="Inter, Segoe UI" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- CONTENT -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel Margin="28,20,28,20" MaxWidth="1400">

                <!-- Status banner -->
                <Border x:Name="statusBanner" Background="#DCFCE7" CornerRadius="8" Padding="14,10" Margin="0,0,0,16" Visibility="Collapsed">
                    <TextBlock x:Name="txtStatus" FontSize="13" FontWeight="SemiBold" Foreground="#166534" FontFamily="Inter, Segoe UI" TextWrapping="Wrap"/>
                </Border>

                <!-- CARD 1: PROFILE INFORMATION -->
                <Border Style="{StaticResource Card}">
                    <StackPanel>
                        <Border BorderBrush="#E5E7EB" BorderThickness="0,0,0,1" Padding="24,14">
                            <TextBlock Text="Profile Information" FontSize="15" FontWeight="SemiBold" Foreground="#111827" FontFamily="Inter, Segoe UI"/>
                        </Border>
                        <StackPanel Margin="24">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="16"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="16"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock FontSize="13" FontWeight="SemiBold" Foreground="#374151" FontFamily="Inter, Segoe UI" Margin="0,0,0,6">
                                        <Run Text="Name"/><Run Text=" *" Foreground="#EF4444"/>
                                    </TextBlock>
                                    <TextBox x:Name="txtName" Style="{StaticResource InputStyle}" MinHeight="40"/>
                                </StackPanel>
                                <StackPanel Grid.Column="2">
                                    <TextBlock FontSize="13" FontWeight="SemiBold" Foreground="#374151" FontFamily="Inter, Segoe UI" Margin="0,0,0,6">
                                        <Run Text="Email"/><Run Text=" *" Foreground="#EF4444"/>
                                    </TextBlock>
                                    <TextBox x:Name="txtEmail" Style="{StaticResource InputStyle}" MinHeight="40"/>
                                </StackPanel>
                                <StackPanel Grid.Column="4">
                                    <TextBlock FontSize="13" FontWeight="SemiBold" Foreground="#374151" FontFamily="Inter, Segoe UI" Margin="0,0,0,6">
                                        <Run Text="Phone"/><Run Text=" *" Foreground="#EF4444"/>
                                    </TextBlock>
                                    <TextBox x:Name="txtPhone" Style="{StaticResource InputStyle}" MinHeight="40"/>
                                </StackPanel>
                            </Grid>

                            <!-- Photo -->
                            <StackPanel Margin="0,16,0,0">
                                <TextBlock Text="Photo" FontSize="13" FontWeight="SemiBold" Foreground="#374151" FontFamily="Inter, Segoe UI" Margin="0,0,0,8"/>
                                <StackPanel Orientation="Horizontal">
                                    <Border Width="100" Height="100" CornerRadius="8" BorderBrush="#E5E7EB" BorderThickness="1" Background="#F9FAFB" ClipToBounds="True">
                                        <Grid>
                                            <Image x:Name="imgPreview" Stretch="Uniform" Margin="4" Visibility="Collapsed"/>
                                            <TextBlock x:Name="txtNoPhoto" Text="No Photo" FontSize="12" Foreground="#9CA3AF" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Grid>
                                    </Border>
                                    <StackPanel Margin="12,0,0,0" VerticalAlignment="Center">
                                        <Button x:Name="btnChoosePhoto" Content="Choose Photo" Style="{StaticResource SecondaryButton}" Click="BtnChoosePhoto_Click" HorizontalAlignment="Left" MinWidth="120"/>
                                        <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                            <Button x:Name="btnPreviewPhoto" Content="Preview" Style="{StaticResource SecondaryButton}" Click="BtnPreviewPhoto_Click" Margin="0,0,8,0" Visibility="Collapsed" MinWidth="80"/>
                                            <Button x:Name="btnRemovePhoto" Content="Remove Photo" Style="{StaticResource SecondaryButton}" Click="BtnRemovePhoto_Click" Visibility="Collapsed" MinWidth="110"/>
                                        </StackPanel>
                                    </StackPanel>
                                </StackPanel>
                            </StackPanel>

                            <Button x:Name="btnSaveProfile" Content="Update Profile" Style="{StaticResource PrimaryButton}" Click="BtnUpdateProfile_Click" Margin="0,20,0,0" HorizontalAlignment="Left"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- CARD 2: CHANGE PASSWORD -->
                <Border Style="{StaticResource Card}">
                    <StackPanel>
                        <Border BorderBrush="#E5E7EB" BorderThickness="0,0,0,1" Padding="24,14">
                            <TextBlock Text="Change Password" FontSize="15" FontWeight="SemiBold" Foreground="#111827" FontFamily="Inter, Segoe UI"/>
                        </Border>
                        <StackPanel Margin="24">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="16"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="16"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock FontSize="13" FontWeight="SemiBold" Foreground="#374151" FontFamily="Inter, Segoe UI" Margin="0,0,0,6">
                                        <Run Text="Current Password"/><Run Text=" *" Foreground="#EF4444"/>
                                    </TextBlock>
                                    <PasswordBox x:Name="pwdCurrent" Style="{StaticResource PwdStyle}" MinHeight="40"/>
                                </StackPanel>
                                <StackPanel Grid.Column="2">
                                    <TextBlock FontSize="13" FontWeight="SemiBold" Foreground="#374151" FontFamily="Inter, Segoe UI" Margin="0,0,0,6">
                                        <Run Text="New Password"/><Run Text=" *" Foreground="#EF4444"/>
                                    </TextBlock>
                                    <PasswordBox x:Name="pwdNew" Style="{StaticResource PwdStyle}" MinHeight="40"/>
                                </StackPanel>
                                <StackPanel Grid.Column="4">
                                    <TextBlock FontSize="13" FontWeight="SemiBold" Foreground="#374151" FontFamily="Inter, Segoe UI" Margin="0,0,0,6">
                                        <Run Text="Confirm New Password"/><Run Text=" *" Foreground="#EF4444"/>
                                    </TextBlock>
                                    <PasswordBox x:Name="pwdConfirm" Style="{StaticResource PwdStyle}" MinHeight="40"/>
                                </StackPanel>
                            </Grid>
                            <Button x:Name="btnSavePassword" Content="Change Password" Style="{StaticResource PrimaryButton}" Click="BtnChangePassword_Click" Margin="0,20,0,0" HorizontalAlignment="Left"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- CARD 3: SECURITY SETTINGS -->
                <Border Style="{StaticResource Card}">
                    <StackPanel>
                        <Border BorderBrush="#E5E7EB" BorderThickness="0,0,0,1" Padding="24,14">
                            <TextBlock Text="Security Settings" FontSize="15" FontWeight="SemiBold" Foreground="#111827" FontFamily="Inter, Segoe UI"/>
                        </Border>
                        <StackPanel Margin="24">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="16"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock FontSize="13" FontWeight="SemiBold" Foreground="#374151" FontFamily="Inter, Segoe UI" Margin="0,0,0,6">
                                        <Run Text="Security Question"/><Run Text=" *" Foreground="#EF4444"/>
                                    </TextBlock>
                                    <ComboBox x:Name="cmbQuestion" Style="{StaticResource ComboStyle}" MinHeight="40" FontFamily="Inter, Segoe UI">
                                        <ComboBoxItem Content="Select a security question"/>
                                        <ComboBoxItem Content="What is your mother's maiden name?"/>
                                        <ComboBoxItem Content="What is the name of your first pet?"/>
                                        <ComboBoxItem Content="What is the name of the town you were born?"/>
                                        <ComboBoxItem Content="What primary school did you attend?"/>
                                        <ComboBoxItem Content="What Is your favorite book?"/>
                                        <ComboBoxItem Content="What was the first company that you worked for?"/>
                                        <ComboBoxItem Content="What is your favorite food?"/>
                                        <ComboBoxItem Content="Where did you meet your spouse?"/>
                                        <ComboBoxItem Content="Where is your favorite place to vacation?"/>
                                        <ComboBoxItem Content="What is the name of the road you grew up on?"/>
                                    </ComboBox>
                                </StackPanel>
                                <StackPanel Grid.Column="2">
                                    <TextBlock FontSize="13" FontWeight="SemiBold" Foreground="#374151" FontFamily="Inter, Segoe UI" Margin="0,0,0,6">
                                        <Run Text="Security Answer"/><Run Text=" *" Foreground="#EF4444"/>
                                    </TextBlock>
                                    <TextBox x:Name="txtAnswer" Style="{StaticResource InputStyle}" MinHeight="40"/>
                                </StackPanel>
                            </Grid>
                            <Button x:Name="btnSaveSecurity" Content="Update Security Settings" Style="{StaticResource PrimaryButton}" Click="BtnUpdateSecurity_Click" Margin="0,20,0,0" HorizontalAlignment="Left"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>

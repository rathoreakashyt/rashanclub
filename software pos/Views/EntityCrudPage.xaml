<UserControl x:Class="RashanKiDukan.Views.EntityCrudPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             xmlns:helpers="clr-namespace:RashanKiDukan.Helpers"
             d:DesignHeight="700" d:DesignWidth="1100"
             Background="{StaticResource BgPageBrush}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ══════════ PAGE HEADER (Sneat) ══════════ -->
        <Border Grid.Row="0" Style="{StaticResource PageHeader}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="&#xE72B;" Style="{StaticResource BtnIcon}" Click="BtnBack_Click"
                            ToolTip="Back" Margin="0,0,10,0"/>
                    <StackPanel VerticalAlignment="Center">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock x:Name="lblTitle" Text="Entity" Style="{StaticResource PageTitle}"/>
                            <TextBlock x:Name="lblSubtitle" Text="List" Style="{StaticResource BreadcrumbText}" Margin="10,3,0,0" VerticalAlignment="Bottom"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                            <TextBlock Text="Home" Style="{StaticResource BreadcrumbText}"/>
                            <TextBlock Text=" / " Style="{StaticResource BreadcrumbText}"/>
                            <TextBlock x:Name="lblBreadcrumbPage" Text="Entity" Style="{StaticResource BreadcrumbText}" Foreground="{StaticResource PrimaryBrush}"/>
                        </StackPanel>
                    </StackPanel>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button x:Name="btnAdd" Content="+ Add New" Style="{StaticResource BtnPrimary}" Margin="0,0,10,0" Click="BtnAdd_Click"/>
                    <Button Content="&#xE72C;" Style="{StaticResource BtnIcon}" Click="BtnRefresh_Click" ToolTip="Refresh"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Content -->
        <Grid Grid.Row="1">
            <!-- LIST PANEL -->
            <Border x:Name="listPanel" Margin="24,20,24,24" Style="{StaticResource Card}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Card header: search -->
                    <Border Grid.Row="0" Style="{StaticResource CardHeader}">
                        <Border Style="{StaticResource SearchBox}" Width="300" HorizontalAlignment="Left">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="&#xE721;" FontFamily="{StaticResource Mdl2}" FontSize="13"
                                           Foreground="{StaticResource TextMutedBrush}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <TextBox Grid.Column="1" x:Name="txtSearch" Style="{StaticResource SearchInput}"
                                         VerticalContentAlignment="Center" TextChanged="TxtSearch_TextChanged"/>
                            </Grid>
                        </Border>
                    </Border>

                    <!-- DataGrid -->
                    <DataGrid x:Name="grid" Grid.Row="1" Style="{StaticResource Table}" Margin="0,0,0,0"
                              SelectionMode="Single" SelectionUnit="FullRow"
                              CanUserAddRows="False" CanUserDeleteRows="False" CanUserResizeRows="False">
                    </DataGrid>

                    <!-- Empty state -->
                    <StackPanel x:Name="emptyState" Grid.Row="1" Visibility="Collapsed"
                                VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,40">
                        <TextBlock Text="&#xE894;" FontFamily="{StaticResource Mdl2}" FontSize="40"
                                   Foreground="{StaticResource TextFaintBrush}" HorizontalAlignment="Center"/>
                        <TextBlock Text="No records found" FontSize="15" FontWeight="SemiBold"
                                   Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Center" Margin="0,8,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- FORM PANEL -->
            <Border x:Name="formPanel" Margin="24,20,24,24" Style="{StaticResource Card}" Visibility="Collapsed">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Form header -->
                    <Border Grid.Row="0" Style="{StaticResource CardHeader}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock x:Name="lblFormTitle" Text="Add" Style="{StaticResource CardTitle}"/>
                            <TextBlock x:Name="lblFormEntity" Text="Entity" Style="{StaticResource SubText}" Margin="8,1,0,0"/>
                        </StackPanel>
                    </Border>

                    <!-- Fields -->
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                        <StackPanel x:Name="formFields" Margin="20,20,20,10"/>
                    </ScrollViewer>

                    <!-- Footer -->
                    <Border Grid.Row="2" Style="{StaticResource CardFooter}">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button x:Name="btnCancel" Content="Cancel" Style="{StaticResource BtnSecondary}" Margin="0,0,10,0" Click="BtnCancel_Click"/>
                            <Button x:Name="btnSave" Content="Save Changes" Style="{StaticResource BtnPrimary}" Click="BtnSave_Click"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
